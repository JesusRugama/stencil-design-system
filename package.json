{
  "name": "stencil-design-system-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "storybook": "turbo run storybook --filter=@jesusrugama/stencil-design-system"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
